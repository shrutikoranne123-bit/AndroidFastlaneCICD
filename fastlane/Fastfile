platform :android do

  desc "Build Internal AAB (No Upload)"
  lane :build_internal do

    gradle(
      task: "bundle",
      build_type: "Release"
    )

    puts "Build successful. AAB generated."
  end

end